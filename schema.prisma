generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Result {
  id        String   @id @default(cuid())
  userId    String
  score     Int
  feedback  String
  createdAt DateTime @default(now())
}